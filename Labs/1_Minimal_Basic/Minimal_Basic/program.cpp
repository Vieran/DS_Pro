#include "program.h"

// error taken func
void error(QString error_msg) {
    // handle the error
}

// show value to get in the command input box
/*
void MainWindow::to_get_input(QString val) {
    ui->COMMAND_text->setText(val);
}
*/